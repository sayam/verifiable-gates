# Tools this repository's CI installs for itself — hash-pinned in requirements.txt
# regenerate: pip-compile --allow-unsafe --generate-hashes --strip-extras \
#   --output-file=pins/dev/requirements.txt pins/dev/requirements.in
ruff
mypy
pytest
pytest-cov
pyyaml
types-PyYAML
pip-audit
xenon
interrogate
build
cyclonedx-bom
setuptools
