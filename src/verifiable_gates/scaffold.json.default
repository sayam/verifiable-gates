{
  "_comment": "Configuration for the gate bundle. Point these at your project's layout; a scan whose path does not exist reports NA rather than failing, so an unused entry is harmless but a wrong one is silent \u2014 check the doctor's output says pass, not NA.",
  "services_path": "app/services",
  "src_path": "app",
  "purge_paths": [
    "app/purge.py"
  ],
  "templates_path": "app/templates",
  "entrypoints": [
    "run.py",
    "wsgi.py",
    "app.py",
    "main.py"
  ],
  "dockerfiles": [
    "Dockerfile"
  ],
  "adr_path": "docs/adr",
  "preflight_jobs": [
    "scans"
  ]
}
