# ทะเบียน gate ของ repo นี้เอง — **ยังว่าง และนั่นเป็นสถานะที่ถูกต้อง ณ ขั้น 1**
#
# repo นี้เป็นบ้านของ *ทะเบียนที่ตรวจสองทิศ* จึงต้องมีทะเบียนของตัวเองตั้งแต่วันแรก
# แต่ **ห้ามใส่แถวก่อนที่ตัวบังคับจะมีอยู่จริง** — ทะเบียนที่ประกาศ gate ซึ่งไม่มีใคร
# บังคับ คือสิ่งเดียวกับที่งานทั้งใบนี้พยายามเลิกทำ · แถวแรกจะมาพร้อมตัวบังคับตัวแรก
# ในขั้น 2 (ตอนที่ checks/ กับ doctor ย้ายเข้ามา)
#
# schema ของไฟล์นี้บังคับด้วย `verifiable_gates.registry` + `tests/test_registry.py`

version: 1
gates: []
